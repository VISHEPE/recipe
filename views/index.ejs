<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/styles.css">
    <title>Recipe Sharing App</title>
</head>
<body>
    <header>
        <h1>Welcome to the Recipe Sharing App</h1>
        <nav>
            <a href="/auth/login">Login</a>
            <a href="/auth/register">Register</a>
        </nav>
    </header>
    <main>
        <p>Share your favorite recipes and explore new ones from the community!</p>
        <% if (recipes.length > 0) { %>
            <section class="recipe-list">
                <% recipes.forEach(recipe => { %>
                    <article class="recipe-item">
                        <h2><%= recipe.title %></h2>
                        <p>Category: <%= recipe.category %></p>
                        <p>Ingredients: <%= recipe.ingredients %></p>
                        <p>Instructions: <%= recipe.instructions.substring(0, 100) %>...</p> <!-- Show a snippet -->
                        <a href="/recipes/view/<%= recipe.id %>">Read More</a>
                    </article>
                <% }); %>
            </section>
        <% } else { %>
            <p>No recipes found. Be the first to add one!</p>
        <% } %>
    </main>
</body>
</html>
